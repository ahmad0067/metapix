@tailwind base;
@tailwind components;
@tailwind utilities;

body {
  background-color: #0a0e11;
  color: #e7edf0;
}

/* Leaflet needs its own stylesheet for tiles/markers to render correctly */
@import "leaflet/dist/leaflet.css";

/* Re-skin the default Leaflet popup to match the dark theme */
.leaflet-popup-content-wrapper,
.leaflet-popup-tip {
  background: #161d22;
  color: #e7edf0;
}
.leaflet-container a.leaflet-popup-close-button {
  color: #9aacb4;
}

/* Thin, quiet scrollbars for the metadata panel */
.scrollbar-thin::-webkit-scrollbar {
  width: 6px;
}
.scrollbar-thin::-webkit-scrollbar-thumb {
  background-color: #33414a;
  border-radius: 999px;
}

@keyframes scan {
  0% { transform: translateY(-100%); }
  100% { transform: translateY(100%); }
}
.animate-scan {
  animation: scan 2.2s ease-in-out infinite;
}
